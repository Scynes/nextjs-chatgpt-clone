import { Flex, Heading } from '@radix-ui/themes';

export const SideBar = () => {
    return (
        <Flex className={ 'h-full w-[260px] px-3' }>
            <Heading>SideBar</Heading>
        </Flex>
    );
}